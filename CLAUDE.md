@AGENTS.md

Read the meta repository's `AGENTS.md` (at `../AGENTS.md` in a nested checkout,
or <https://github.com/yegamble/vizra/blob/main/AGENTS.md>) before this file: it
binds every Vizra repository and takes precedence.

Read the relevant ADRs and the assigned execution plan on demand; do not preload
the whole repository. Before context compaction, preserve acceptance IDs,
branch/SHA, modified files, exact verification commands and results, blockers,
and the next concrete action in the execution plan under the meta repository's
`docs/plans/`. Never replace evidence with a narrative success summary.
