# VZ-FOUND-007 — required demonstrations, red then green

Produced by `./docs/evidence/fixtures/demonstrate.sh` on 2026-09-21T01:30:48Z,
darwin/arm64, against commit 99830ed6503fcd5b7b7da1204649b18f8d1991cd.

The script refuses to run on a dirty tree and restores every mutation with
`git checkout --`; the clean `git status` at the end of the transcript is the
proof it did. A verifier can re-run it from a clean checkout.

Each demonstration is GREEN, then RED against one controlled mutation, then
GREEN again once the mutation is reverted.

```
host:      Darwin/arm64
go:        go version go1.27.1 darwin/arm64
go.mod go: 1.26.0
HEAD:      99830ed6503fcd5b7b7da1204649b18f8d1991cd
date:      2026-09-21T01:30:25Z

========================================================================
GREEN baseline: make fixtures-verify
========================================================================
==> fixtures-verify
fixtures-verify: ok — 12 fixtures, 1791508 bytes, generator 1 (a34db2c4f1bf), toolchain go1.27.1
  every fixture regenerated byte-identically, the generator source digest matches,
  the committed codec inputs match, and the toolchain is the pinned one.
exit=0

========================================================================
RED 1: one byte of one fixture changed
========================================================================
flipped one bit in testdata/fixtures/jpeg-exif-orientation6-gps.jpg
==> fixtures-verify
fixtures-verify: FAILED with 2 problem(s)

  fixture-on-disk
      jpeg-exif-orientation6-gps.jpg in testdata/fixtures has sha256 10ee0bb7313d4930eee2f196caee8d55f68255fa1c9b4eb9a7136d52c6a504e5, the manifest records 44863b49cf10222c13a85dca77ea7f6b1950905b5e48dfc3ff37a24c368185e4 — the corpus on disk is not the corpus the manifest describes
  not-compared
      the fixture bytes were NOT regenerated and compared, because something cheaper already disagrees with the manifest. Fix the problems above and run again; a byte comparison made under a different pin, a different generator source or beside a stale corpus is not evidence about anything.

fixturegen: the corpus does not match the committed manifest
exit status 1
make: *** [fixtures-verify] Error 1
exit=2  (expected non-zero, naming the file)

========================================================================
GREEN 1: the fixture regenerated
========================================================================
==> fixtures-verify
fixtures-verify: ok — 12 fixtures, 1791508 bytes, generator 1 (a34db2c4f1bf), toolchain go1.27.1
  every fixture regenerated byte-identically, the generator source digest matches,
  the committed codec inputs match, and the toolchain is the pinned one.
exit=0

========================================================================
RED 2: the generator changed without the manifest being re-pinned
========================================================================
==> fixtures-verify
fixtures-verify: FAILED with 2 problem(s)

  generator-source
      the generator source digest is 966328a46ebef37f…, the manifest records a34db2c4f1bf9c7e…. The generator changed and the manifest was not regenerated: run `make fixtures-manifest` and review the fixture hashes in the diff.
  not-compared
      the fixture bytes were NOT regenerated and compared, because something cheaper already disagrees with the manifest. Fix the problems above and run again; a byte comparison made under a different pin, a different generator source or beside a stale corpus is not evidence about anything.

fixturegen: the corpus does not match the committed manifest
exit status 1
make: *** [fixtures-verify] Error 1
exit=2  (expected non-zero: generator-source AND the fixture bytes)

========================================================================
GREEN 2: the generator change reverted
========================================================================
==> fixtures-verify
fixtures-verify: ok — 12 fixtures, 1791508 bytes, generator 1 (a34db2c4f1bf), toolchain go1.27.1
  every fixture regenerated byte-identically, the generator source digest matches,
  the committed codec inputs match, and the toolchain is the pinned one.
exit=0

========================================================================
RED 3: a pinned byte-influencing version bumped without a re-pin
========================================================================
--- 3a. the go.mod pin raised without re-pinning the corpus ---
go 1.26.2
==> fixtures-verify
fixtures-verify: FAILED with 2 problem(s)

  not-compared
      the fixture bytes were NOT regenerated and compared, because something cheaper already disagrees with the manifest. Fix the problems above and run again; a byte comparison made under a different pin, a different generator source or beside a stale corpus is not evidence about anything.
  toolchain-pin
      go.mod's language version is 1.26.2, the manifest records 1.26.0. That directive sets the standard library's GODEBUG compatibility defaults, and under GOTOOLCHAIN=auto it also selects which toolchain runs the generator — and the toolchain demonstrably changes these bytes. Re-pin with `make fixtures-manifest` and review which fixture hashes moved.

fixturegen: the corpus does not match the committed manifest
exit status 1
make: *** [fixtures-verify] Error 1
exit=2  (expected non-zero: toolchain-pin)
--- 3b. the recorded codec tool version, with the bytes left alone ---
bumped the recorded ffmpeg version in CodecInputs()
==> fixtures-verify
fixtures-verify: FAILED with 3 problem(s)

  codec-tool-pin
      internal/fixtures/codec/webm-short.webm was produced by "ffmpeg 9.0, darwin/arm64", the manifest records "ffmpeg 8.1, darwin/arm64". A codec pin moved without the corpus being re-pinned.
  generator-source
      the generator source digest is c20e46b8c7056494…, the manifest records a34db2c4f1bf9c7e…. The generator changed and the manifest was not regenerated: run `make fixtures-manifest` and review the fixture hashes in the diff.
  not-compared
      the fixture bytes were NOT regenerated and compared, because something cheaper already disagrees with the manifest. Fix the problems above and run again; a byte comparison made under a different pin, a different generator source or beside a stale corpus is not evidence about anything.

fixturegen: the corpus does not match the committed manifest
exit status 1
make: *** [fixtures-verify] Error 1
exit=2  (expected non-zero: codec-tool-pin)

========================================================================
GREEN 3: both pins restored
========================================================================
==> fixtures-verify
fixtures-verify: ok — 12 fixtures, 1791508 bytes, generator 1 (a34db2c4f1bf), toolchain go1.27.1
  every fixture regenerated byte-identically, the generator source digest matches,
  the committed codec inputs match, and the toolchain is the pinned one.
exit=0

========================================================================
RED 4: the fixtures lane deleted from the required-checks manifest
========================================================================
removed the 'fixtures' line from .github/required-checks.txt
  FAIL  required lane 'fixtures' is MISSING from required-checks.txt.
        It is a floor lane (scripts/ci-required-guard.py, FLOOR_LANES).
        Deleting a manifest line does not shrink the gate; it turns this lane red.
ci-required-guard: FAILED
ci-required-guard:
  ok    required check 'append-only' resolves to a job
  ok    required check 'build-test' resolves to a job
  ok    required check 'cache-matrix' resolves to a job
  ok    required check 'govulncheck' resolves to a job
  ok    required check 'docker-build' resolves to a job
  ok    floor lane 'append-only' runs on pull_request
  ok    floor lane 'append-only' carries no continue-on-error
  ok    floor lane 'build-test' runs on pull_request
  ok    floor lane 'build-test' carries no continue-on-error
  ok    floor lane 'cache-matrix' runs on pull_request
  ok    floor lane 'cache-matrix' carries no continue-on-error
  ok    floor lane 'fixtures' runs on pull_request
  ok    floor lane 'fixtures' carries no continue-on-error
  ok    floor lane 'govulncheck' runs on pull_request
  ok    floor lane 'govulncheck' carries no continue-on-error
  ok    floor lane 'docker-build' runs on pull_request
  ok    floor lane 'docker-build' carries no continue-on-error
  ok    the test-race lane selects the whole module (PKGS = ./...)
  ok    the test-race target uses $(PKGS)
  ok    all 2 -run selections are non-empty
  ok    every job runs on GitHub-hosted ubuntu-24.04
  ok    every third-party action is pinned to a commit SHA
exit=1  (expected non-zero: the guard FLOOR names the missing lane)

========================================================================
GREEN 4: the lane restored
========================================================================
exit=0

========================================================================
Final state
========================================================================
==> fixtures-verify
fixtures-verify: ok — 12 fixtures, 1791508 bytes, generator 1 (a34db2c4f1bf), toolchain go1.27.1
  every fixture regenerated byte-identically, the generator source digest matches,
  the committed codec inputs match, and the toolchain is the pinned one.
(a clean status above means every mutation was restored)
```
