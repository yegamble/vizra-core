# GNU Make 4.3, Python 3.12.3, tree 2dab194badac035d2efb3b3e915bf624440fa64c
make-integrity-guard: /src
  gate targets: ci
  ok    every line of the pinned makefile(s) is one of the allowed shapes (the allowlist grammar, before make): 56 blank/comment, 11 assignment, 25 phony, 25 rule, 72 recipe
  ok    make runs only on REVIEWED bytes: the 1 file(s) it will read (Makefile) match the sha256 pins in .github/pinned-makefiles.yml, checked before make was invoked. Those bytes are not inert — reading them runs the reviewed parse-time call(s) Makefile:22 `COMMIT      ?= $(shell git rev-parse --short=12 HEAD 2>/dev/null || echo unknown)`; Makefile:23 `BUILT_AT    ?= $(shell date -u +%Y-%m-%dT%H:%M:%SZ)`. Review of a Makefile together with its pin is the control; CODEOWNERS is advisory.
  ok    [--workflow (strict)] MAKEFLAGS/GNUMAKEFLAGS/MFLAGS unset; MAKELEVEL/MAKE_RESTARTS/MAKEOVERRIDES/MAKECMDGOALS absent; MAKEFILES/BASH_ENV/ENV unset; SHELL is a real shell
  ok    `make` resolves to /usr/bin/make (type -t: file; real file /usr/bin/make, named make, in an approved system directory). This does not inspect the program's CONTENT: a forwarding stub planted there by an earlier step is outside what this guard can see.
  ok    [--workflow] none of the 6 variable(s) the makefiles take from the environment is set: ['BUILT_AT', 'COMMIT', 'GO', 'GOFLAGS', 'RELEASE', 'SQLC']
  closure: 11 target(s); 10 reached through prerequisites (fmt-check, vet, lint-imports, migrate-lint, config-template-check, openapi-verify, sqlc-verify, ci-guard, fixtures-verify, test-race)
  ok    make will read 1 makefile(s), from the pinned bytes: Makefile
  ok    the root Makefile pins SHELL and .SHELLFLAGS to the approved values, and nothing else assigns them
  ok    gate target `ci` is defined exactly once (Makefile:38)
  ok    gate target `fmt-check` is defined exactly once (Makefile:43)
  ok    gate target `vet` is defined exactly once (Makefile:59)
  ok    gate target `lint-imports` is defined exactly once (Makefile:65)
  ok    gate target `migrate-lint` is defined exactly once (Makefile:70)
  ok    gate target `config-template-check` is defined exactly once (Makefile:79)
  ok    gate target `openapi-verify` is defined exactly once (Makefile:84)
  ok    gate target `sqlc-verify` is defined exactly once (Makefile:89)
  ok    gate target `ci-guard` is defined exactly once (Makefile:103)
  ok    gate target `fixtures-verify` is defined exactly once (Makefile:131)
  ok    gate target `test-race` is defined exactly once (Makefile:157)
  ok    every one of the 11 gate closure target(s) has one explicit rule and is declared .PHONY, so make searches no implicit, pattern or .DEFAULT rule for any of them
  ok    `make -q Makefile` (every pinned makefile a goal, so -q applies to each) reports none would be remade; every later make command names them as goals so -n applies to them. -q runs no ordinary recipe — only `+`/`$(MAKE)` lines, which can come only from the pinned bytes
  ok    make's own MAKEFILE_LIST ['Makefile'] is exactly the pinned set
  ok    `ci`: make resolves SHELL to the approved /usr/bin/env bash
  ok    `ci`: make resolves .SHELLFLAGS to the approved '-eu -o pipefail -c'
  ok    `ci`: `.ONESHELL:` is not in effect
  ok    `ci`: .RECIPEPREFIX is not set
  ok    `ci`: `.SECONDEXPANSION:` is not in effect
  ok    `ci`: no `.IGNORE`, no `.DEFAULT` recipe, no `.EXTRA_PREREQS`
  ok    `ci`: MAKEFLAGS carries nothing beyond this guard's own -pn ('np')
  ok    `ci`: no expanded gate command ends in a `|| true`-family suffix
  ok    `ci`: make reports no duplicate-definition override
  ok    make's own database: each of the 11 gate closure target(s) has ONE readable entry whose 34 recipe line(s) equal the pinned rule's and carry no `-`/`+` prefix, sub-make or swallowing suffix
  ok    the gate closure make reports from its database equals the text closure (11 target(s))
  ok    no gate recipe line expands to a `-` or `+` prefix (34 distinct line(s) from the text and make's database, leading variable references resolved from the database)
  ok    make's own .PHONY list covers all 11 gate closure target(s)
make-integrity-guard: passed (1 gate target(s); make ran 4 time(s), only on the pinned bytes of Makefile)
exit=0
