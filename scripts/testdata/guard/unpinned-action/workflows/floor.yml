name: floor
on:
  pull_request:
  merge_group:
permissions:
  contents: read
jobs:
  append-only:
    name: append-only
    runs-on: ubuntu-24.04
    steps:
      - run: echo ok
  build-test:
    name: build-test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - run: make ci
  cache-matrix:
    name: cache-matrix
    runs-on: ubuntu-24.04
    steps:
      - run: echo ok
  govulncheck:
    name: govulncheck
    runs-on: ubuntu-24.04
    steps:
      - run: make govulncheck
  docker-build:
    name: docker-build
    runs-on: ubuntu-24.04
    steps:
      - run: docker build .
